{
  "dxt_version": "0.1",
  "name": "grafana-dxt",
  "version": "v0.11.2",
  "description": "Grafana MCP server integration for Desktop Extensions",
  "author": {
    "name": "Cedric Ziel"
  },
  "user_config": {
    "grafana_url": {
      "type": "string",
      "title": "Grafana URL",
      "description": "The URL of your Grafana instance (e.g., https://myinstance.grafana.net)",
      "required": true,
      "default": "http://localhost:3000"
    },
    "grafana_service_account_token": {
      "type": "string",
      "title": "Grafana Service Account Token",
      "description": "Service account token for authenticating with Grafana",
      "required": true,
      "sensitive": true
    },
    "grafana_org_id": {
      "type": "string",
      "title": "Grafana Organization ID",
      "description": "Numeric organization ID for multi-org Grafana setups (leave empty for default org)"
    },
    "grafana_extra_headers": {
      "type": "string",
      "title": "Extra HTTP Headers",
      "description": "JSON object of custom HTTP headers for reverse proxy or custom auth (e.g., {\"X-Custom-Auth\": \"value\"})"
    }
  },
  "server": {
    "type": "binary",
    "entry_point": "server/mcp-grafana",
    "mcp_config": {
      "command": "${__dirname}/server/mcp-grafana",
      "env": {
        "GRAFANA_URL": "${grafana_url}",
        "GRAFANA_SERVICE_ACCOUNT_TOKEN": "${grafana_service_account_token}",
        "GRAFANA_ORG_ID": "${grafana_org_id}",
        "GRAFANA_EXTRA_HEADERS": "${grafana_extra_headers}"
      },
      "platform_overrides": {
        "darwin": {
          "command": "${__dirname}/server/mcp-grafana-darwin",
          "env": {
            "GRAFANA_URL": "${grafana_url}",
            "GRAFANA_SERVICE_ACCOUNT_TOKEN": "${grafana_service_account_token}",
            "GRAFANA_ORG_ID": "${grafana_org_id}",
            "GRAFANA_EXTRA_HEADERS": "${grafana_extra_headers}"
          }
        },
        "linux": {
          "command": "${__dirname}/server/mcp-grafana-linux",
          "env": {
            "GRAFANA_URL": "${grafana_url}",
            "GRAFANA_SERVICE_ACCOUNT_TOKEN": "${grafana_service_account_token}",
            "GRAFANA_ORG_ID": "${grafana_org_id}",
            "GRAFANA_EXTRA_HEADERS": "${grafana_extra_headers}"
          }
        },
        "win32": {
          "command": "${__dirname}/server/mcp-grafana-windows.exe",
          "env": {
            "GRAFANA_URL": "${grafana_url}",
            "GRAFANA_SERVICE_ACCOUNT_TOKEN": "${grafana_service_account_token}",
            "GRAFANA_ORG_ID": "${grafana_org_id}",
            "GRAFANA_EXTRA_HEADERS": "${grafana_extra_headers}"
          }
        }
      }
    }
  },
  "compatibility": {
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ],
    "claude_desktop": ">=0.1.0"
  },
  "license": "Apache-2.0"
}
